{
  "name": "use-single-state",
  "version": "1.0.1",
  "description": "A React hook for using a unified state, with optional state diff logging",
  "main": "dist/index.js",
  "types":"dist/index.d.ts",
  "scripts": {
    "build":"rimraf dist && tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "deploy":"tsc && npm deploy",
    "link":"npm link",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["react","hooks","react-hooks","typescript"],
  "author": "Callum Jenkins",
  "license": "ISC",
  "devDependencies": {
    "@types/react":"^16.8.8",
    "prettier":"^1.16.4",
    "rimraf":"^2.6.3",
    "typescript":"^3.3.4000"
  },
  "dependencies": {
		"deep-diff": "^1.0.2",
    "react":"^16.8.5"
  }
}
